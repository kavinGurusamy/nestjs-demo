import { DatabaseExceptionFilterFilter } from './database-exception-filter.filter';

describe('DatabaseExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new DatabaseExceptionFilterFilter()).toBeDefined();
  });
});
